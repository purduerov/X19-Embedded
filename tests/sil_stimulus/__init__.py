"""
SIL stimulus harness: reusable fixtures for the native SIL bridge server tests.

Run the framing tests from the repository root with::

    python -m unittest tests.sil_stimulus.test_protocol_roundtrip -v
"""
